"""经营归因分析系统的服务应用包。

分层（越界即缺陷，见 `docs/02-流程与规范.md` §2.1）：

* ``api/``      HTTP 路由与依赖注入，只做参数校验与响应组装，禁止写算法。
* ``services/`` 用例编排（会话状态机、步骤流、权限与审计）。
* ``warehouse/`` 合成数仓的库结构、口径参数、生成器与校验（P2 交付）。
* ``sandbox/``  受限子进程执行器（P3 交付）。
* ``reports/``  七段式报告组装与三格式导出（P5 交付）。

算法一律放在 ``packages/attribution``，本包只允许调用，不允许重写。
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
