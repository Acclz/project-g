# 项目zz · 经营归因分析系统

面向企业核心经营场景的智能多维归因分析系统：指标树因子拆解、自动假设生成、统计检验验证、业务大事件因果关联，支持多轮下钻追问、What-If 策略推演与结构化归因报告导出。

一句话：**仪表盘只能告诉你"销售额掉了 8%"，这个项目要回答"这 8% 里，多少是渠道结构变了、多少是某个品类断货、多少是噪声"。**

> **状态：P4 已完成（假设生成与事件关联），准备进入 P5。** 本仓库遵循"文档先行、按阶段交付、指标必须可复现"的做法：README 里不会出现任何未经实测的数字。
> 仓库地址：<https://github.com/Acclz/project-g>

## 现在做到哪了

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| P1 | 文档定稿（五件套 + 对照矩阵 + 指标字典初版 + 调研来源清单 + 术语手册初版） | ✅ 完成 |
| P2 | 合成数仓与指标树引擎（含真值预埋） | ✅ 完成（实测数字见下） |
| P3 | 分解算法与数据沙箱 | ✅ 完成（实测数字见下） |
| P4 | 假设生成、统计检验与事件关联 | ✅ 完成（实测数字见下） |
| P5 | 会话与三栏工作台前端（五页面） | 🔄 下一步 |
| P6 | 评测与扩写 | ⏳ 未开始 |
| P7 | 材料与部署 | ⏳ 未开始 |

## 文档从哪读

入口是 [`docs/README.md`](docs/README.md)。如果只想先懂业务，直接读
[`docs/guide/03-业务术语与口径手册.md`](docs/guide/03-业务术语与口径手册.md)——它把 GMV、UV、CVR、LMDI 这类术语逐条翻译成一句人话，不需要行业经验。

## 技术栈（已定）

Python 3.12 · FastAPI · SQLAlchemy 2 · SQLite（数仓与业务库分文件，见 `docs/04-决策记录.md` D-22）· Vue 3 + Vite · 云端 DeepSeek（仅用于假设草案与报告文字）· 受限子进程数据沙箱（**不依赖 Docker**）

## P2 实测数字（2026-09-15，本机 Python 3.12.7 / SQLite 3.45.3）

```powershell
cd D:\项目zz
python services\attr-api\scripts\generate_warehouse.py --profile full --reset   # 生成数仓
python services\attr-api\scripts\verify_warehouse.py                            # 校验（对账/指标 SQL/恒等式/真值）
python -m pytest -p no:cacheprovider                                            # 56 个测试
```

| 项 | 实测值 |
| --- | --- |
| 事实表行数 | 4,152,997 行（电商日表 1,009,706 / 订单明细 1,920,179 / 快消日表 1,223,040） |
| 索引条目 | 13,468,481 条；`dw.db` 628.6 MB |
| 生成耗时 | 38.2 s（第二次 40.4 s）；重置（删库重建）0.08 s |
| 可复现 | 同种子重跑整库指纹一致：`2a73035e…f09f2`（sha256，按天汇总） |
| 校验 | 6/6 通过：聚合表与明细表逐日对账、23 个指标节点 SQL 出数、8 条恒等式相对残差 < 1e-9、4 条真值可复算 |

> 这组数字只允许在**同样命令 + 同样种子**下重跑复现；完整口径与解释见
> [`docs/03-进度与质量.md`](docs/03-进度与质量.md) 第 6 节。

## P3 实测数字（同一台机器，完整档 4,152,997 行）

```powershell
cd D:\项目zz
python services\attr-api\scripts\run_adversarial.py                       # 沙箱对抗（E4）
python services\attr-api\scripts\demo_decompose.py                        # 分解链路端到端（E2）
python services\attr-api\scripts\bench.py --label p3_full --repeat 50     # 性能基准
```

| 项 | 实测值 |
| --- | --- |
| 沙箱对抗（E4） | 22 用例（20 应拦截 + 2 正控）：拦截率 **100.0%**、漏拦 0、误拦 0、审计留痕 22/22 |
| 守恒（E2） | 混合树随机 120 组 100% 通过（相对误差 < 1e-9）；真实数仓上 4 次完整分解全部守恒 |
| 大盘单日 | P50 **2.0 ms** / P95 2.1 ms（基准 ≤2000 ms） |
| 一级拆解 + 守恒 | P50 **131 ms** / P95 152 ms（基准 ≤3000 ms，不含模型调用） |
| 单次沙箱探查 | P50 **563 ms** / P95 569 ms（基准 ≤5000 ms） |
| 测试与静态检查 | ruff 全绿、**133 passed**；6 项基准错误率全部 0% |

## P4 实测数字（同一环境，完整档）

```powershell
cd D:\项目zz
python services\attr-api\scripts\demo_attribution.py                    # L1 链路端到端（含落库）
python services\attr-api\scripts\eval_attribution.py --label p4_full    # E1 / E3 评测
```

| 项 | 实测值 | 参考目标 |
| --- | --- | --- |
| E1 Top-1 命中率 | **75.0%**（3/4，样本 4 条预埋真因） | ≥70% |
| E1 Top-3 命中率 | **100%**（4/4） | ≥90% |
| E1 贡献额相对误差中位数 | **6.8%**（最大 21.2%） | ≤5%，**未达标**（口径差异见 `docs/03-进度与质量.md` §8.5） |
| E3 伪相关误纳率 | **0.0%**（0/5 陷阱） | ≤10% |
| E1 守恒 | 4/4 守恒，最大相对残差 5.6e-14 | <1e-9 |
| 测试与静态检查 | ruff 全绿、**171 passed** | — |

> 诚实声明：E1/E3 是**首轮基线**，样本小（4 条真因 / 5 条陷阱）且数据为合成；
> 复现命令、口径差异与已知偏差见 [`docs/03-进度与质量.md`](docs/03-进度与质量.md) 第 8 节。

## 数据说明（诚实声明）

* 本项目的多维数仓为**合成数据**，由固定种子的生成器产出，可复现。
* 合成数据**预埋已知真因**，因此归因准确率可以量化；但对外必须声明"准确率类指标基于合成数据集"。
* 守恒误差、沙箱越权拦截率、并发一致性、接口 P95 等指标与数据真假无关，属硬指标。

## 参考资料来源（只读，不在本仓库内）

* `D:\项目实战参考资料\项目实战(2).md`：题目与验收标准来源（第 2.1 节）。
* `D:\项目z`：作品一（教育知识库平台），本仓库只复用其工程脚手架与规范，不复用业务代码。
